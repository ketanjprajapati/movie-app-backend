# movie-app-backend

steps
-git clone "https://github.com/ketanjprajapati/movie-app-backend.git"
-cd movie-app-backend
-npm install -f
-npm start


-npm test
-comment the server.js below metioned code
app.listen(PORT, () => {
    console.log(`Server is running on port ${PORT}`);
});

-uncomment  module.exports = app;

-